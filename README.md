# Activity app!
Creating a small desktop app where you can get some ideas on what to do!

Current progress: run the app from the terminal with `npm run start`

After installing .dmg type the following in the terminal:
`cd ~/Downloads`
`xattr -cr "Dates-and-Activities.dmg"`
